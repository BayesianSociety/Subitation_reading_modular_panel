services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DB_PATH=/app/storage/app.db
    volumes:
      - sqlite_data:/app/storage
    restart: unless-stopped

volumes:
  sqlite_data:
